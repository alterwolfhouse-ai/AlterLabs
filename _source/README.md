# AlterLabs homepage source

This directory contains the maintained Vite/React source for the generated homepage assets in `/assets`.

## Build

```powershell
npm install
npm run build
```

Copy `dist/index.html` to the repository root and copy the generated `dist/assets/*` files into `/assets`.

Static service, solution, guide and blog pages remain at the repository root. Their generators live in `/scripts`.
